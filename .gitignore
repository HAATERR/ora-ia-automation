node_modules/
.next/
.env
.env.local

# Reportes de alta (contienen IDs de la instancia)
config/clients/*.provisioned.json

# Tokens / secretos
secrets/
*.pit

# Claude Code (config local de permisos; launch.json sí se versiona)
.claude/settings.local.json
**/.claude/settings.local.json

# Logs
*.log
